# Database Client & Schemas (`src/db/`)

PostgreSQL database connections, migrations, and ORM/Query models.

## 📁 Database Directory Breakdown

- `index.js`: PostgreSQL client pool connection setup.
- `schema.sql`: PostgreSQL relational table definitions (`users`, `profiles`, `jobs`, `applications`, `test_scores`, `sla_timers`).
